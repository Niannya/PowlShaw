# 日常维护指南

这份文件面向不想经常修改程序逻辑的维护者。

## 1. 修改文章和活动

启动网站后访问：

- 网站：`http://localhost:3000`
- 后台：`http://localhost:3000/admin`

文章、活动、评论账号和评论都应在后台维护，不需要修改代码。

文章管理页支持按标题或网址别名搜索，并按所属活动折叠分组；也可直接选择某个活动筛选。
文章保存后会自动公开，不需要手动选择“草稿/已发布”；页面中的本机草稿只用于防止编辑内容
意外丢失，不会写入网站数据库。

活动编辑页的“参与作品与分组”采用所见即所得的单列卡片编辑器：先创建一级分组，需要
嵌套时再在对应分组里点击“添加子分组”，然后把文章卡片直接拖进目标层级。在同一分组内
拖动可调整文章顺序；拖动标题左侧的手柄可以调整同层分组顺序。完成后点击
“保存分组与顺序”。不需要分组的文章放在“未分组”。不同活动不必使用相同结构，空分组
也会保留。

需要增加作品时，点击活动编辑页右上角的“添加新文章”。新文章会自动关联当前活动并排在
“未分组”末尾，保存文章后会返回活动编辑页，再将它拖到需要的分组即可。

活动分为“破晓”和“其他”。新建或编辑活动时可修改“活动类型”；现有第一届至第九届
破晓已归入“破晓”，2019夏活等写作活动归入“其他”。第二届资料中的 4 篇夏活作品已移至
独立的“2019夏活”活动，原文章网址与评论不变。历届破晓的作品目录以轮次为外层、
科幻组/奇幻组为内层：预赛、第一轮、第二轮、更多轮次、半决赛、决赛、外场、其他依次排列；
决赛友谊赛放在“决赛 / 友谊赛”，没有标明轮次的友谊赛才放在“其他”。
不确定的组别显示为“未分组”，不强行推断。没有作品的轮次不显示。其他类型活动也可以自由创建
自己的分组。活动管理列表按“破晓 / 其他”两组显示，可在**同组内**
拖动卡片或选择目标位置来排序。活动开始和结束时间只填写年月；结束月份会按该月最后一天
处理。文章发布日期只填写年月日。

每张活动卡片右上角都有“在首页展示”按钮。首页直接显示所选活动的标题、简介、日期和
链接；更换首页活动不会修改活动档案。需要改活动本身时，使用同一行的“编辑”。

“列国纪”使用专属专题页。进入它的活动编辑页后，“列国纪专题设置”可以直接上传或替换
世界地图、暂时隐藏地图、修改图片替代文字、独立地图页标题和专题页各区标题。图片限制与
普通图片上传相同：JPG、PNG、GIF 或 WebP，最大 5MB。修改后点击“保存专题设置”，再用
右上角的“查看专题页”或“查看地图页”核对结果。活动名称、简介、时间和活动介绍正文仍在
页面最上方的活动表单中维护。

登录的受邀账号可以在“列国纪”专题页或独立地图页点击“添加节点”，再在地图上选择位置并
填写名称、描述和公开备注。编辑框中还可按标题或网址别名搜索已公开文章；一个节点可以不关联
文章，也可以按选择顺序关联多篇。节点保存后立即公开，访客可从节点详情打开这些文章；创建者
点击节点后，可从详情菜单选择“移动位置”，在地图上点击可以粗调位置，继续拖动节点可以微调，
最后点击“保存位置”。创建者也可修改文字、文章关联或删除节点。普通账号只能操作自己创建的
节点，不能修改其他账号的节点。

需要标出一片范围时，点击“添加区域”，依次在地图上添加至少三个顶点；可撤销最后一个顶点，
也可直接拖动已经放下的顶点微调边界。完成圈选后填写名称、颜色、描述和公开备注；填写表单时
仍可拖动地图上的顶点。区域创建者可以编辑信息、重新圈选或删除；其他账号只能查看。

地图上至少有两个节点时，登录账号还可以点击“添加关系”，依次选择两个节点，再填写关系
名称、关系说明和公开备注。关系可以连接其他账号创建的公开节点，但关系本身仍归创建者所有，
只有创建者能修改或删除。关系线表达抽象联系，不代表道路、航线或其他真实地理路线。同一对
节点可以建立多条不同关系。节点删除后，与它相连的关系会从当前地图隐藏。

需要记录某个阶段的世界状态时，进入“列国纪”的活动编辑页，在“世界快照”中选择日期，按需
填写名称和说明，然后点击“记录当前世界”。系统会同时保存当时的地图图片、节点、区域、关系
和节点文章关联；之后继续编辑当前地图不会改写历史快照。公开专题页和独立地图页会出现
“查看世界状态”下拉框，访客可以在当前世界和各份历史快照之间切换。快照是只读的，只能由
管理员手动创建或删除；系统不会自动定时生成。同一日期只能保留一份快照。

未登录访客只能浏览，不能修改当前世界或历史快照。

已有评议表、评议结果、统计及晋级名单等资料在各活动页的“评议资料下载”区单独提供，
不与作品原稿混在一起。日常增补时进入活动编辑页，在“评议资料与附件”中选择文件；可以
填写公开显示名称和分组说明，上传后还可以改名、调整顺序或删除。单个文件最大 20MB，支持
XLSX、XLS、CSV、DOCX、DOC、RTF、PDF、TXT、JPG 和 PNG。这里上传的文件会公开下载，
不要上传作品原稿或私密资料。

如果需要从原始资料目录批量整理，仍可在 `破晓相关/破晓相关/` 中增补文件，然后先运行
`python scripts/import_event_documents.py --dry-run --list` 检查归属与范围，再运行
`pnpm db:import-event-documents`。脚本不会删除或修改原件，并在更新数据库前自动备份。

## 2. 创建评论账号

网站不提供公开注册。管理员登录后台后，进入“用户”页面创建受邀账号：

```text
http://localhost:3000/admin/users
```

创建后，请通过私下渠道把用户名和初始密码发给对方。账号的“管理员备注”只在后台显示，
可以用来记录真实姓名、来源或联系方式，方便辨认。可以随时停用账号、重设密码或删除账号。
新账号首次登录时必须修改初始密码；修改后，其他设备上的旧登录状态会自动失效。
删除账号时，该账号发表的评论也会一并删除。

评论管理页面：

```text
http://localhost:3000/admin/comments
```

可在这里隐藏、恢复或永久删除评论。

评论管理还支持内容、账号、文章、状态和日期筛选，以及 CSV/JSON 导出。用户删除的评论
会先进入可恢复状态。管理操作记录位于 `http://localhost:3000/admin/audit`。文章和活动
在新建、修改或删除时还会写入数据库内容快照，误操作后可依据这里的记录和数据库备份恢复。

文章编辑页面可以将评论区设为“开放”“关闭但保留旧评论”或“完全隐藏”。账号管理页面
可以设置禁言截止时间与原因。

### 修改管理员密码

管理员登录后台后，进入“后台安全”：

```text
http://localhost:3000/admin/security
```

输入当前密码和两次新密码即可修改。新密码至少 8 位；修改成功后，其他设备上的后台登录
状态会失效。新密码只以加密哈希保存在数据库中，不会明文写入 `.env.local`。

## 3. 上传和编辑文章

新建或编辑文章时，可以直接输入正文，也可以上传不超过 20MB 的 `.docx`。DOCX 中常见的
段落、标题、列表、表格、链接和图片会转换到编辑器；旧版 `.doc` 需要先用 Word 另存为
`.docx`。导入不会自动发布，必须检查预览后再保存。

文章和活动正文均可切换“富文本 / Markdown”编辑模式，也可以通过“导入 .md”选择本机的
`.md`、`.markdown` 文件（最大 2MB）。Markdown 源文会随正文保存，下次打开仍可直接编辑；
在富文本模式修改正文后，将改为保存富文本内容。切换旧的复杂富文本内容到 Markdown 前，
建议先备份并检查预览。

编辑页面会把未提交内容临时保存在当前浏览器，并在意外离开时提醒。浏览器草稿不是服务器
备份；完成编辑后仍要点击“保存文章”。

文章网址统一使用“活动网址 + 编号”。例如第一届破晓活动是 `poxiao-01`，其中的文章依次为
`poxiao-01-01`、`poxiao-01-02`；夏活和冬活分别使用 `summer-年份`、`winter-年份`。
新建文章时只选择一个所属活动，后台会自动生成下一个编号，不需要填写网址别名。已经发布的
网址不要随意修改；确需修改时，系统会保留旧地址并自动跳转到新地址。

首页欢迎区各行文字和站内公告都在后台“站点设置”维护：

```text
http://localhost:3000/admin/settings
```

建站日期固定显示在网页底端，不需要日常修改。来访计数显示在首页欢迎语的最后一行，不保存 IP
或其他个人信息；同一浏览器每 12 小时只计一次，避免反复刷新造成数字快速增长。

首页的饼干和红茶数量保存在数据库中；普通结果计入“吃掉/喝掉”，每次点击另有千分之一
概率触发摔碎结果。全站总数包含未登录访客，已登录账号的个人数量可以在后台“评论账号”
页面查看。

## 4. 修改颜色和外观

样式按用途拆分在 `src/styles/`。先找对应页面，再改相应文件：

```text
src/styles/base.css           全站变量、纸张背景、链接等基础样式
src/styles/site.css           前台布局、导航、首页与点心动画
src/styles/content.css        文章和活动内容
src/styles/community.css      评论、账号、通用按钮和搜索
src/styles/admin-shell.css    后台布局、导航、概览
src/styles/admin-content.css  后台列表、表单、编辑器
src/styles/responsive.css     窄屏适配
```

`src/app/globals.css` 只控制这些文件的加载顺序；后面的样式可能覆盖前面的规则，
不要随意打乱顺序。建议先搜索 CSS 类名，再修改颜色、字体、间距或阴影。

数据库连接在 `src/lib/db.ts`，表结构和旧库兼容迁移在 `src/lib/db-schema.ts`；
日常维护不需要修改它们，更不要直接删除数据库文件。文章编辑器的本机草稿逻辑在
`src/components/admin/useArticleAutoSave.ts`。

## 5. 修改后检查

开发模式下，保存文件后网页会自动刷新：

```powershell
pnpm dev
```

完成修改后运行：

```powershell
pnpm check
pnpm build
```

- `check`：检查格式、Lint、TypeScript 类型和小型回归测试，不自动改动代码。
- 如果格式检查不通过，再运行 `pnpm format` 自动整理缩进和换行。
- `build`：确认正式版本能够构建。

## 6. 数据和备份

- 当前数据库：`data/poxiao.db`
- 上传图片：`public/uploads/`
- 数据库备份：`data/backups/`
- 已下架的原始文档公开副本：`data/private-archive-files/`（网站无法直接访问）
- 登录密码和会话密钥：`.env.local`

备份网站时，至少复制 `data/poxiao.db`、`public/uploads/` 和 `.env.local`；若需要保留
原始文档，也备份 `破晓相关/` 或 `data/private-archive-files/`。

## 7. 活动资料批量导入

活动资料已经整理进网站，平时直接在后台维护即可，不需要重复运行脚本。若原始目录有
批量变更，需要按原规则重新整理，可执行：

```powershell
python -m pip install -r scripts/requirements-activities.txt
pnpm db:import-activities
```

脚本读取 `破晓相关/破晓相关/`，不会修改原文件；它会更新已经导入的文章并保留评论。
旧版 `.doc`、`.rtf` 需要本机安装 Microsoft Word。导入后请查看：

```text
破晓相关/整理输出/其余活动-导入报告.md
data/remaining_activities_import_report.json
```

“其它稿件合集（上传文章请发在这里）”不属于活动，脚本不会导入。
已导入活动的通用“活动档案”说明块和文章末尾的原文件下载链接已移除；评议表等管理资料
仍保留在本地原始资料夹，不作为公开作品展示。旧的原文件公开副本与第一届资料包已移至
`data/private-archive-files/`；正文配图仍可正常显示。
重新导入时，脚本仍会将第二届目录里文件名带“夏活”的 4 篇作品归到“2019夏活”，并按上述
轮次顺序整理历届破晓。不要手动修改原始文件名来改变已发布文章的网址。

## 8. 上传公开源码仓库

Git 仓库只用于保存程序代码、测试、维护文档和必要静态素材。`.env.local`、`data/` 中的数据库
与备份、`public/uploads/` 和 `破晓相关/` 都是本地或部署数据，不应提交。首次提交或
推送前运行：

```powershell
pnpm release:check
pnpm check
pnpm build
```

如果 `release:check` 报告私密文件或本机绝对路径，不要强制添加；应先修正忽略规则或移除公开
文件中的本地信息。
