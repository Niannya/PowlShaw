# 破晓网站：项目状态与新对话交接

> 最近更新：2026-09-22  
> 工作目录：本地项目根目录  
> 本文件面向接手后续开发的新 Codex 对话。日常操作说明另见 `MAINTENANCE.md`。

## 1. 新对话开始时

按以下顺序阅读：

1. `AGENTS.md`
2. `PROJECT_STATUS.md`（本文件）
3. `MAINTENANCE.md`
4. `SRC_FILE_GUIDE.md`（需要定位代码时）
5. 与当前需求直接相关的代码

不要仅凭 Next.js 的既有知识修改项目。当前使用 Next.js 16.3.4；写代码前应查看
`node_modules/next/dist/docs/` 中与目标功能对应的本地文档。

## 2. 项目定位和已经确定的产品约定

- 网站名为“破晓”，用途是展示写作组文章和历届活动档案。
- 公开页面采用古早个人主页、纸张和档案馆风格；参考过 Sakinorva 的复古网页气质。
- 后台应与前台保持同一套视觉语言，避免现代化卡片、圆角 SaaS、默认浏览器按钮等突兀 UI。
- 不保留作者系统或作者展示功能。
- 不再提供文章分类功能；旧分类和关联记录只保留在数据库中用于兼容，不在前后台使用。
- 不开放公众注册。评论账号只能由管理员创建，账号备注仅管理员可见。
- 用户密码和管理员密码至少 8 位；密码不可出现在 URL、日志或页面明文中。
- 活动分为“破晓”和“其他”。
- 历届破晓的目录以“轮次在外、分组在内”的结构展示：第一轮、第二轮、更多实际轮次、
  半决赛、决赛、外场、其他；每个轮次内部再分科幻组和奇幻组。不能把真实存在的更多轮次
  强行合并为两轮。
- “决赛友谊赛”等内容属于决赛的子分组；外场作品放在决赛之后。无法可靠判断的内容不要
  擅自归类。
- 第二届资料中的夏活已经拆成独立“2019夏活”。
- 活动中的评议表、评议结果、统计表、晋级名单等作为“评议资料下载”提供；作品原稿不提供
  下载，文章末尾也不要添加“下载作品”。
- 首页展示活动直接从已有活动中选择。活动卡片右上角的“在首页展示”只更换首页引用，
  不能修改或复制活动档案。
- 文章网址使用“活动网址 + 编号”：第一届破晓为 `poxiao-01-01`、`poxiao-01-02`；
  夏活、冬活分别使用 `summer-年份`、`winter-年份`。旧网址通过重定向表兼容。
- 文章标题作为链接时使用正常的链接视觉（用户最后明确要求改回链接形式）。同类位置的
  按钮、链接、下拉框需要全站统一。
- 首页欢迎语、公告在后台站点设置中维护。建站日期固定在页脚；不显示“最后更新”组件。
- 首页访问计数显示在欢迎语末行。同一浏览器 12 小时计一次，不保存 IP。
- 饼干和红茶的统计点击后才显示，文案分开；极低概率触发摔碎动画。数据库同时记录全站
  总数和已登录用户个人数量。

## 3. 当前已经实现的功能

### 前台

- 首页欢迎区、站内公告、当前活动、最近更新、年份档案。
- 文章列表、搜索、随机文章、文章详情。
- 活动专题及嵌套作品目录。
- 登录用户评论：回复、提及通知、编辑、软删除、置顶、分页及链接识别。
- 饼干/红茶交互、访问计数、响应式布局。

### 后台

- 文章的新建、编辑、删除、搜索和按活动分组。
- DOCX 导入；富文本/Markdown 双模式；`.md` 上传；预览、本机草稿和离开提醒。
- 活动的新建、编辑、删除、类型、排序及首页展示选择。
- 活动编辑页使用单列、可嵌套的所见即所得分组编辑器：
  - 可新建一级分组和子分组；
  - 文章可拖入任意层级并在组内排序；
  - 同一层级的分组可拖动排序；
  - 空分组会保存；
  - 最新修改已删除大块“把文章拖到这里”占位条。空分组只显示标题栏，仍可把文章拖到
    分组标题栏完成归类。
- 可从活动编辑页直接“添加新文章”；新文章自动关联当前活动，保存后返回活动页。
- 活动编辑页可直接上传、改名、排序和删除评议资料及其他公开附件；支持常见办公文档、
  PDF、文本和图片，单个文件最大 20MB。
- “列国纪”已有专属宽版专题页和世界地图，地图在专题页内可直接缩放、拖动，也可进入独立
  页面浏览。登录用户可在地图上创建、查看、编辑、拖动和删除自己的抽象节点，不能改动他人
  节点；还可选择任意两个公开节点创建抽象关系，维护自己的关系名称、说明和公开备注。节点与
  关系保存后立即公开，未登录访客只读。管理员可在活动编辑页上传/替换或隐藏地图，并维护专题
  各区标题。多边形区域、文章关联和定期世界快照尚未实现。
- 评论管理、受邀账号管理、管理员改密、站点设置、操作记录和内容快照。
- 后台样式已改成与前台一致的复古档案风格。

更详细的站长操作步骤见 `MAINTENANCE.md`。

## 4. 技术栈

- Next.js 16.3.4（App Router）
- React 19.2.8
- TypeScript 5.9
- SQLite + `better-sqlite3`
- Route Handlers 作为后端 API
- 原生 CSS，无外部 UI 框架
- pnpm 11.19.0

这是一个前后端一体的本地 Next.js 项目，不是 WordPress。

## 5. 关键目录和文件

```text
src/app/(site)/                 公开页面
src/app/admin/                  后台页面
src/app/api/                    后端接口
src/components/                 前台共用组件
src/components/admin/           后台表单和编辑器
src/components/admin/EventArticleManager.tsx
                                活动嵌套分组及文章拖放编辑器
src/config/site.ts              常用固定文案、导航、页脚
src/lib/db.ts                   SQLite 连接
src/lib/db-schema.ts            建表和向后兼容迁移
src/lib/queries.ts              公开页面查询
src/styles/                     按用途拆分的全站样式
data/poxiao.db                  当前正式内容数据库
data/backups/                   数据库备份
data/private-archive-files/     不公开的原始文件副本
public/uploads/                 后台上传内容
scripts/                        导入、整理和迁移脚本
tests/                          轻量回归测试
破晓相关/破晓相关/              用户提供的原始活动资料
```

CSS 分工：

```text
src/styles/base.css             变量、背景、全站基础
src/styles/site.css             前台框架、首页和点心动画
src/styles/content.css          文章及活动正文
src/styles/community.css        评论、登录、按钮和搜索
src/styles/admin-shell.css      后台框架与导航
src/styles/admin-content.css    后台表单、列表和编辑器
src/styles/responsive.css       窄屏适配
```

`src/app/globals.css` 只控制样式加载顺序，不要随意调整。

## 6. 当前数据快照

2026-09-21 对 `data/poxiao.db` 的只读检查结果：

- 文章：668
- 活动：27
- 活动与文章关联：668
- 活动分组：78
- 评议资料：89
- 专题地图设置：1（列国纪）
- 地图节点：0（当前正式库；随后台和用户维护变化）
- 地图关系：0（当前正式库；随用户维护变化）
- 旧文章分类记录：2（分类功能已停用，数据仅保留兼容）
- 评论账号：2
- 评论：0
- 作者：0
- SQLite `integrity_check`：`ok`
- 外键违规：0

内容会随后台维护变化，以上数字只用于交接核对，不应写死进页面逻辑。`README.md` 中若出现
不同的固定数量，应以数据库实时结果为准。

与最近活动分组改造有关的备份：

```text
data/backups/poxiao-before-event-groups-complete-20260920-210439.db
data/backups/poxiao-after-event-groups-20260920-210428.db
data/backups/poxiao-before-event-map-admin-20260921-211304.db
data/backups/poxiao-after-event-map-admin-20260921-212539.db
data/backups/poxiao-before-map-relations-20260921-225825.db
```

前者是改造前的可靠回退点，后者是加入活动分组后的快照。恢复数据库前必须先备份当前
`data/poxiao.db`，不要直接覆盖。

## 7. 运行方式（用户习惯 CMD）

在 CMD 中：

```bat
cd /d "<项目目录>"
pnpm dev
```

然后访问：

- 前台：`http://localhost:3000`
- 后台：`http://localhost:3000/admin`
- 评论账号登录：`http://localhost:3000/login`

正式构建后运行：

```bat
cd /d "<项目目录>"
pnpm build
pnpm start
```

如果 `pnpm` 无法识别，应先安装/启用 Node.js 和 Corepack，而不是改项目代码。

交接时端口 3000 没有监听进程；新对话若需要打开网站，应先启动开发或正式服务器。

## 8. 修改后的必做验证

```bat
pnpm check
pnpm build
```

`pnpm check` 会依次执行 Prettier 检查、ESLint、TypeScript 和 Node 测试。必要时再用浏览器
检查目标页面，特别是后台拖放、窄屏布局和富文本编辑器。

最近一次验证（列国纪抽象关系功能完成之后）：

- `pnpm check`：通过，24 项测试全部通过。
- `pnpm build`：通过，Next.js 生产构建成功。
- 独立地图页 HTTP 检查：200；浏览器检查通过，关系曲线、名称、详情、创建表单和所有者操作
  均可正常显示。
- 关系接口集成检查：未登录拦截、创建、公开读取、本人修改、阻止他人修改、服务端页面渲染和
  软删除全部通过；测试使用数据库副本，正式库没有留下测试关系。
- SQLite `integrity_check`：`ok`；外键违规：0。

## 9. 数据、安全和隐私边界

- `.env.local` 含敏感配置。不要读取后复制到回复或文档，也不要提交或分享。
- 管理员密码已可在 `/admin/security` 修改；数据库只存密码哈希。
- 不要公开用户备注、密码、会话、真实姓名、评论导出或本地绝对资料路径。
- 不要直接手工编辑或删除 `data/poxiao.db`。
- 改表结构时在 `src/lib/db-schema.ts` 中做可重复执行的增量迁移，并先备份数据库。
- 上传图片在 `public/uploads/`，数据库和上传目录应一起备份。
- 当前目录已经初始化为 Git 仓库，但尚未创建首次提交或配置远端。`.gitignore` 会排除真实
  环境变量、运行数据库、备份、上传附件和原始活动资料；首次提交前运行 `pnpm release:check`。

## 10. 不要随意执行的命令

以下命令用于旧站或原始活动资料的批量导入，不是普通启动命令：

```bat
pnpm db:import
pnpm db:reset
pnpm db:import-activities
pnpm db:import-event-documents
pnpm db:migrate-slugs
```

尤其 `pnpm db:reset` 会重建内容。除非用户明确要求恢复/重导资料，并且已完成备份和预演，
否则不要运行。

## 11. 后续对话的工作原则

1. 先复述并核对用户指出的具体界面位置，再读现有实现。
2. 小范围、可回退地修改，不要顺手重做已确认的产品决定。
3. 任何数据库批量修改都先备份、先统计、后验证。
4. 对 UI 修改同时检查前台、后台以及响应式样式，避免重新引入用户已经否定的风格。
5. 不要把空占位条、重复编辑入口、裸露且风格不一致的链接或默认下拉框重新加回来。
6. 完成后给出修改文件、验证命令和结果；如果启动了网站，给出可访问地址。
